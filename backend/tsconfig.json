{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",                        // JavaScript language version
    "module": "commonjs",                      // Module system
    "esModuleInterop": true,                   // Allows default imports from CommonJS modules
    "forceConsistentCasingInFileNames": true,  // Enforce consistent casing in file names
    "strict": true,                            // Enable all strict type-checking options

    /* JavaScript Support */
    "allowJs": true,                           // Allow JavaScript files to be a part of the project
    "checkJs": false,                          // Disable type-checking for JS files (set true if you want it)

    /* Source Map Support */
    "sourceMap": true,                         // Generate .map files
    "outDir": "./dist",                        // Redirect output structure to the dist folder
    "rootDir": "./src",                        // Specify root folder of input files

    /* Module Resolution */
    "moduleResolution": "node",                // Match Node.js module resolution
    "resolveJsonModule": true,                 // Allow importing .json files
    "allowSyntheticDefaultImports": true,      // Allow default imports from modules without default export

    /* Type Declaration Support */
    "declaration": true,                       // Generate corresponding .d.ts files

    /* Interop Constraints */
    "isolatedModules": true,                   // Ensure that each file can be transpiled independently

    /* Code Emission and Helpers */
    "removeComments": true,                    // Remove comments in the emitted output

    /* New JS Features */
    "useDefineForClassFields": true,           // Use define semantics for class fields

    /* Type Checking */
    "noImplicitAny": true,                     // Raise error on expressions and declarations with an implied 'any' type
    "strictNullChecks": true,                  // Enable strict null checks
    "noUnusedLocals": true,                    // Report unused local variables
    "noUnusedParameters": true,                // Report unused function parameters
    "noImplicitThis": true,                    // Raise error on 'this' expressions with an implied 'any' type
    "alwaysStrict": true                       // Parse in strict mode and emit "use strict" for each source file
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
